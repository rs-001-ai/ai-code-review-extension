{
    "name": "ai-code-review-extension",
    "version": "1.0.0",
    "description": "AI Code Review Extension for Azure DevOps",
    "scripts": {
        "build:task": "cd tasks/AICodeReviewTask && npm install && npm run build",
        "build": "npm run build:task",
        "package": "tfx extension create --manifest-globs vss-extension.json",
        "publish": "tfx extension publish --manifest-globs vss-extension.json",
        "publish": "tfx extension publish --manifest-globs vss-extension.json"
    },
    "keywords": [
        "azure-devops",
        "extension",
        "code-review",
        "ai",
        "openai"
    ],
    "author": "Rachit Singhal",
    "license": "MIT",
    "devDependencies": {
        "tfx-cli": "^0.16.0"
    }
}
